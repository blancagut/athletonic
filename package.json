{
  "name": "athletonic",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deals": "node scripts/deals-engine.mjs",
    "generate": "node scripts/generate-home.mjs",
    "generate:deals": "npm run deals && npm run generate",
    "test": "find api -name '*.js' -exec node --check {} ';'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.0",
    "stripe": "^22.1.1"
  },
  "devDependencies": {
    "vercel": "^54.2.0"
  }
}
