{
  "name": "athletonic",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "node scripts/generate-home.mjs",
    "test": "node --check api/checkout.js && node --check api/stripe-webhook.js && node --check api/orders/session.js && node --check api/orders/lookup.js && node --check api/returns/request.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.0",
    "stripe": "^22.1.1"
  },
  "devDependencies": {
    "vercel": "^54.2.0"
  }
}
